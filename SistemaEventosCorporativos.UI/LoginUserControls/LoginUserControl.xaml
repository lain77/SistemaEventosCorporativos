<UserControl x:Class="SistemaEventosCorporativos.UI.LoginUserControls.LoginUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#111111">
        <!-- Card central -->
        <Border Width="550"
                Padding="20"
                Background="#1C1C1C"
                CornerRadius="12"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                BorderBrush="#2B3F74"
                BorderThickness="2">
            <StackPanel>
                <TextBlock Text="Login"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,20"/>

                <!-- Usuário -->
                <TextBlock Text="Usuário:" 
                           Foreground="White" 
                           FontSize="14"/>
                <TextBox x:Name="txtUsuario"
                         Height="35"
                         Margin="0,5,0,15"
                         Background="#2B2B2B"
                         Foreground="White"
                         BorderThickness="0"
                         Padding="8"/>

                <!-- Senha -->
                <TextBlock Text="Senha:" 
                           Foreground="White" 
                           FontSize="14"/>
                <PasswordBox x:Name="txtSenha"
                             Height="35"
                             Margin="0,5,0,20"
                             Background="#2B2B2B"
                             Foreground="White"
                             BorderThickness="0"
                             Padding="8"/>

                <!-- Botão Login -->
                <Button Content="Entrar"
                        Height="45"
                        Background="#2B3F74"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold"
                        Cursor="Hand"
                        Click="BtnLogin_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
