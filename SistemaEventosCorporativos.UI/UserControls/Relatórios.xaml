<UserControl x:Class="SistemaEventosCorporativos.UI.UserControls.Relatórios"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <!-- Estilo minimalista para botões arredondados -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#1B1B23"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="8"
                                Padding="10,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#1F618D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F5F5F5" Margin="10">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="400">

            <TextBlock Text="Relatórios &amp; Dashboards"
               FontSize="22"
               FontWeight="Bold"
               Foreground="#2C3E50"
               TextAlignment="Center"
               Margin="0,0,0,20"/>

            <Button Content="📅 Agenda dos Participantes" Style="{StaticResource RoundedButton}" Margin="0,5,0,5"
        Click="BtnAgendaParticipantes_Click"/>
            <Button Content="👥 Fornecedores Mais Utilizados" Style="{StaticResource RoundedButton}" Margin="0,5,0,5"
        Click="BtnFornecedoresMaisUtilizados_Click"/>
            <Button Content="📊 Tipos de Participantes" Style="{StaticResource RoundedButton}" Margin="0,5,0,5"
        Click="BtnTiposParticipantes_Click"/>
            <Button Content="💰 Saldo de Orçamento" Style="{StaticResource RoundedButton}" Margin="0,5,0,5"
        Click="BtnOrcamentoEventos_Click"/>

        </StackPanel>

    </Grid>
</UserControl>
