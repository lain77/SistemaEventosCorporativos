<UserControl x:Class="SistemaEventosCorporativos.UI.UserControls.AgendaParticipantes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#F5F5F5" Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <!-- Lista de participantes -->
            <ColumnDefinition Width="10"/>
            <!-- Espaço -->
            <ColumnDefinition Width="*"/>
            <!-- Eventos -->
        </Grid.ColumnDefinitions>

        <!-- Participantes -->
        <StackPanel Grid.Column="0">
            <TextBlock Text="Participantes"
                       FontSize="18" FontWeight="Bold"
                       Margin="0,0,0,10"/>
            <ListBox x:Name="listParticipantes"
                     DisplayMemberPath="Nome"
                     SelectionChanged="ListParticipantes_SelectionChanged"
                     Height="400"/>
        </StackPanel>

        <!-- Eventos do participante -->
        <StackPanel Grid.Column="2">
            <TextBlock Text="Eventos"
                       FontSize="18" FontWeight="Bold"
                       Margin="0,0,0,10"/>
            <DataGrid x:Name="dataGridEventos"
                      AutoGenerateColumns="False" Height="400"
                      CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True"
                      AlternatingRowBackground="#F9F9F9" RowBackground="White"
                      GridLinesVisibility="Horizontal" HeadersVisibility="Column" ColumnHeaderHeight="30">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Evento" Binding="{Binding NomeEvento}" Width="*"/>
                    <DataGridTextColumn Header="Início" Binding="{Binding DataInicio}" Width="120"/>
                    <DataGridTextColumn Header="Fim" Binding="{Binding DataFim}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
